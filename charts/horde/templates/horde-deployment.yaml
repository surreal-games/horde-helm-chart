{{- $name := .Chart.Name }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ lower $name }}-app
  namespace: {{ lower $name }}
spec:
  replicas: {{ .Values.deployment.replicaCount }}
  {{- if .Values.deployment.strategy }}
  strategy:
    {{- with .Values.deployment.strategy }}
      {{ toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ lower $name }}-app
  template:
    metadata:
      {{- if .Values.deployment.metadata.annotations }}
      annotations:
        {{- with .Values.deployment.metadata.annotations }}
          {{ toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      labels:
        app: {{ lower $name }}-app
        app.kubernetes.io/name: {{ lower $name }}
        {{- if .Values.deployment.metadata.labels }}
          {{- with .Values.deployment.metadata.labels }}
            {{ toYaml . | nindent 8 }}
          {{- end }}
        {{- end }}
    spec:
      {{- if .Values.deployment.container.serviceAccount }}
      serviceAccountName: {{ .Values.deployment.container.serviceAccount }}
      {{- else }}
      serviceAccountName: {{ lower $name }}-service-account
      {{- end }}
      volumes:
        - name: shared-data
          emptyDir: { }
      terminationGracePeriodSeconds: {{ .Values.deployment.container.terminationGracePeriodSeconds }}
      containers:
        - name: {{ .Values.deployment.container.name }}
          image: {{ .Values.deployment.container.image.name }}:{{ .Values.deployment.container.image.tag }}
          imagePullPolicy: {{ .Values.deployment.container.image.pullPolicy }}
          resources:
          {{- if .Values.deployment.container.resources }}
            {{- with .Values.deployment.container.resources }}
              {{ toYaml . | nindent 12 }}
            {{- end }}
          {{- else }}
            limits:
              cpu: 1
              memory: 4Gi
            requests:
              cpu: 1
              memory: 4Gi
          {{- end }}
          ports:
            - name: {{ lower $name }}-http-port
              containerPort: {{ .Values.service.http.port }}
            - name: {{ lower $name }}-grpc-port
              containerPort: {{ .Values.service.grpc.port }}
          volumeMounts:
            - name: shared-data
              mountPath: /app/config
          envFrom:
            - configMapRef:
                name: {{ lower $name }}-configmap
            - configMapRef:
                name: {{ lower $name }}-p4-configmap
            - secretRef:
                name: {{ lower $name }}-p4-secret
            - secretRef:
                name: {{ lower $name }}-mongodb-secret
            {{- range .Values.deployment.container.envFromConfigMaps }}
            - configMapRef:
                name: {{ . }}
            {{- end }}
            {{- range .Values.deployment.container.envFromSecrets }}
            - secretRef:
                name: {{ . }}
            {{- end }}
          env:
            - name: Horde__redisConnectionConfig
              value: {{ .Values.redis.host }}:{{ .Values.redis.port }}
            {{- if .Values.mongodb.install_documentdb_cert }}
            - name: Horde__databasePublicCert
              value: "/app/config/global-bundle.pem"
            {{- end }}
            {{- range .Values.deployment.container.env }}
            - {{ .name }}: {{ .value }}
            {{- end }}
          {{- if .Values.deployment.container.lifecycle }}
          lifecycle:
            {{- with .Values.deployment.container.lifecycle }}
              {{ toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
      initContainers:
        - name: unreal-horde-p4-trust
          image: ubuntu:noble
          command: ["/bin/sh", "-c"]
          args:
            - |
              apt-get update
              apt-get install -y curl gnupg
              curl -fs https://package.perforce.com/perforce.pubkey | gpg --dearmor -o /usr/share/keyrings/perforce.gpg
              echo "deb [signed-by=/usr/share/keyrings/perforce.gpg] https://package.perforce.com/apt/ubuntu noble release" > /etc/apt/sources.list.d/perforce.list
              apt-get update
              apt-get install -y p4-cli
              {{- range .Values.perforce.trust_servers }}
              echo "Trusting P4 server {{ . }}"
              p4 -p {{ . }} trust -y
              {{- end }}
          envFrom:
            - configMapRef:
                name: {{ lower $name }}-p4-configmap
            - secretRef:
                name: {{ lower $name }}-p4-secret
          volumeMounts:
            - name: shared-data
              mountPath: /app/config
        {{- if .Values.mongodb.install_documentdb_cert }}
        - name: unreal-horde-docdb-cert
          image: public.ecr.aws/docker/library/bash:5.3
          command: ["wget", "https://truststore.pki.rds.amazonaws.com/global/global-bundle.pem", "-P", "/app/config/"]
          volumeMounts:
            - name: shared-data
              mountPath: /app/config
        {{- end }}