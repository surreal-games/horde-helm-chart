apiVersion: v1
kind: Service
metadata:
  name: {{ lower .Chart.Name }}-http-service
  namespace: {{ lower .Chart.Name }}
spec:
  type: LoadBalancer
  selector:
    app: {{ lower .Chart.Name }}
  ports:
    - name: {{ lower .Chart.Name }}-api-port
      port: {{ .Values.horde.service.ports.api }}
      targetPort: {{ .Values.horde.service.ports.api }}