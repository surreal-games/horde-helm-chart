{{- if .Values.horde.ingress.grpc.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ lower .Chart.Name }}-grpc-ingress
  namespace: {{ lower .Chart.Name }}
  {{- if .Values.horde.ingress.grpc.annotations }}
  annotations:
    {{- with .Values.horde.ingress.grpc.annotations }}
      {{ toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.horde.ingress.grpc.className }}
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ lower .Chart.Name }}-service
                port:
                  number: {{ .Values.horde.service.ports.grpc }}
{{- end }}